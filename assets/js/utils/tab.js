function tabLink(click, box) {
  const dataClick = document.querySelectorAll(click)
  const dataBox = document.querySelectorAll(box)

  console.log('dataClick', dataClick)
  console.log('dataBox', dataBox)
  
}